node_modules
build
coverage

scripts/keys.json
scripts/*.csv
scripts/genome.txt
scripts/operonOutput.json
**/.DS_Store
.vscode

TODO.md

# Environment files
.env
.env.local
.env.production
.env.*
# .env.development is intentionally committed: it only carries non-secret
# local-dev defaults (localhost endpoints, feature gate) for the
# docker-compose local-emulation setup — no real credentials.
!.env.development

grv_v2_docs
.claude
